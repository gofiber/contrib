name: "Test fibersentry"

on:
  push:
    branches:
      - master
      - main
    paths:
      - 'v3/fibersentry/**'
  pull_request:
    paths:
      - 'v3/fibersentry/**'

jobs:
  Tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version:
          - 1.25.x
    steps:
      - name: Fetch Repository
        uses: actions/checkout@v5
      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '${{ matrix.go-version }}'
      - name: Run Test
        working-directory: ./v3/fibersentry
        run: go test -v -race ./...
